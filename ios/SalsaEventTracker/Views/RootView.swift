// TEMPORARY stub — ios/SalsaEventTracker/Views/RootView.swift
// Will be replaced in Task 7
import SwiftUI
struct RootView: View {
    @Environment(AppModel.self) private var model
    var body: some View { Text("Loading…") }
}
